{
  "name": "sketch",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-p start:build start:serve",
    "start:build": "tsc --watch",
    "start:serve": "browser-sync start --server -w --ignore src/**/*"
  },
  "devDependencies": {
    "@types/p5": "^0.9.0",
    "browser-sync": "^2.26.12",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.5.4"
  }
}
